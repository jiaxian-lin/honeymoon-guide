# 澳洲、新西兰自由行攻略 · 编辑说明

## 打开与分享

双击 `index.html`，在浏览器中打开即可，无需启动服务。默认在线街道地图需要联网；可用地图右上角的下拉菜单切换为离线简图。文字、图片、地点与行程路线都保存在本地。分享时请将整个文件夹一起复制或压缩，保留相对位置。

- `content.js`：所有章节文字、图片路径、地点与路线。
- `assets/style.css`：样式；`assets/app.js`：交互。
- `assets/images/`：本地图片，当前插画为示例，并非实景。
- `assets/world.js`、`assets/detail.js`、`assets/lakes.js`：本地真实地理底图。

## 编辑已有内容

使用文本编辑器打开 `content.js`。前半部分是准备攻略和全程总览；`dayDrafts` 是 12 天的日期、标题、行程主线与地图区域。`itineraryPlaces` 是地点坐标库，`dailyLocations` 按日期列出地点顺序和原图时间；修改这两个区域即可维护每日地图。

日期保留原图月日，没有确定年份。12 天均已有地点标记、地点名称和分段路线；未确认的住宿和活动集合点列在当天的待确认提示中，不会虚构坐标。点击景点卡片会放大地图，点击标记可查看坐标、说明及来源。09.07 的原图时间冲突已经提示。

修改后保存并刷新浏览器。修改副本更稳妥；注意英文逗号、引号和方括号。

## 新增章节

最简单的方法是在 `content.js` 最后追加以下内容；左侧目录、章数和前后导航会自动更新。章节顺序取决于数组顺序；同组章节应放在一起。`id` 必须唯一。`type` 只使用 `article`（纯图文）或 `map`（地图）。

```js
window.TRIP.chapters.push({
  id: 'my-notes',
  type: 'article',
  group: '其他攻略',
  navTitle: '餐厅收藏',
  title: '两个人的餐桌',
  intro: '想一起尝试的味道。',
  blocks: [
    { type: 'heading', text: '晚餐计划' },
    { type: 'paragraph', text: '这里写自己的攻略正文。' },
    { type: 'list', items: ['第一项', '第二项'] },
    { type: 'note', text: '这里放提示。' },
    { type: 'image', src: 'assets/images/journey.svg', alt: '图片说明', caption: '照片注释与来源' }
  ]
});
```

地图章节在以上结构中改成 `type: 'map'`，并增加以下字段：

```js
region: 'southIsland', // world / sydney / southIsland
places: [
  { id: 'a', name: '地点 A', coordinates: [-45.026,168.656], time: '09:30', description: '自己的安排' },
  { id: 'b', name: '地点 B', coordinates: [-45.034,168.660], description: '时间可以不填' }
],
routes: [
  { mode: 'walk', points: ['a','b'], schematic: true }
]
```

所有坐标是 **[纬度, 经度]**（不是 GeoJSON 的经度在前格式）。路线的 `points` 对应同章节地点 `id`。`mode` 可为 `flight`（飞机）、`transit`（公交／地铁）、`walk`（步行）、`drive`（驾驶）；分别以金色、紫色、绿色、蓝色表示。

示意线默认为虚线。已有核实后的真实路线时，可用 `coordinates: [[纬度,经度], ...]` 代替 `points`，并设置 `schematic: false`；不要仅连接起终点便称作真实道路。多个路线可分别设置，地图自动包含所有坐标。

可选 `bounds: [[南界纬度,西界经度],[北界纬度,东界经度]]` 固定初始范围；否则根据地点与路线自动适配，空路线回退到 `region`。如果地图章节没有地点和路线，会正常显示地区地图及待补充状态。

## 图片与留白

将照片复制到 `assets/images/`，然后填写相对路径。使用 JPG、PNG、WebP 或 SVG；使用本地文件，不要填写网络图片地址。图片不存在时显示“图片待补充”，不会出现破图图标。无图片可直接省略 image 块。

`blocks`、`places`、`routes` 均可省略或为空数组。`emptyText` 可定制待补充提示；填完后删除此字段。把地图章节改成 `article` 后地图自动隐藏；原先的地图字段不会影响图文显示。

## 地图范围与来源

默认使用 OpenFreeMap 在线矢量街道地图，可以查看道路、建筑和周边地点名称，放大后显示更多细节；门牌号是否可见取决于当地地图数据覆盖。点击行程地点卡片会放大到附近街区。底图本身不计算路线；分段交通功能另行计算步行和驾驶路线。

在线底图通过 OpenFreeMap 获取样式、字体和地图数据，无需 API 密钥。首次加载失败、网络断开或浏览器不支持 WebGL 时自动切换离线简图，可点击“重试在线地图”。手动选择的地图模式会在浏览器允许本地存储时记住。

在线地图设置集中在 `content.js` 的 `window.TRIP.map` 中：`defaultBase` 可设为 `online` 或 `offline`，`style` 是兼容的 MapLibre 样式地址。浏览器已保存的手动选择优先于默认值。

离线简图为真实地理轮廓地图，不含街道详情。澳大利亚与新西兰使用 Natural Earth 1:10m 国家多边形，并补充区域湖泊数据，世界其他地区使用 1:110m 国家多边形。悉尼城市级放大时海岸细节有限，这是地理数据比例尺的限制。

地点使用已保存的地标或区域坐标，来源见 `地图位置来源.md`；驾驶与步行使用 OSRM 路网规划，飞机使用航线示意；公交／地铁没有核实线路时不绘制连线。道路路线为规划参考，准确入口、实时交通、公交班次与航班时刻仍需出发前核对。离线模式不请求在线地图；在线模式按当前视野加载地理数据，不批量预下载地图。

- OpenFreeMap 在线地图：https://openfreemap.org/quick_start/ ；地图数据 © OpenStreetMap contributors / OpenMapTiles。
- MapLibre GL JS 5.6.2 与 Leaflet 绑定 0.1.4：运行脚本保存在 `assets/vendor/`，许可证同目录。
- Natural Earth：公共领域地理数据，https://www.naturalearthdata.com/about/terms-of-use/
- 数据下载源：https://github.com/nvkelso/natural-earth-vector/tree/master/geojson
- Leaflet 1.9.4：https://leafletjs.com/ ，许可见 `assets/vendor/LEAFLET-LICENSE.txt`。
- `journey.svg` 与 `favicon.svg`：本项目绘制的示例矢量图。

路由服务说明：https://routing.openstreetmap.de/about.html

### 图文章节中的表格与来源链接

签证攻略已填入 2026 年 9 月旅行的 DIY 博文，正文仍集中在 `content.js` 的 `visa` 章节。

- 表格：`{ type: 'table', caption: '表格说明', headers: ['项目', '费用'], rows: [['示例项目', '金额']] }`。窄屏可在表格内部横向滚动。
- 来源链接：`{ type: 'links', items: [{ label: '官方说明', url: 'https://example.com' }] }`。链接在新标签页打开，接受 HTTP/HTTPS 地址，也支持 `assets/documents/文件名.pdf` 本地文件（文件名使用字母、数字、下划线或连字符）。
- 表格、段落与链接标签均按纯文本显示，无需填写 HTML。
- 费用按博文记录的申请日期及参考汇率展示；调整收费或汇率时，同步更新合计、人民币估算、差价和核对日期。

### 交通攻略资源

交通已拆为 `transport-australia`、`transport-new-zealand` 两章；行李独立保留在 `packing`。新西兰已填入自驾交规博文；行李已填入实际打包与用品体验。

图片块添加 `fit: "contain"` 可完整展示标志或示意图，不裁切。

- `assets/images/contactless-card.png`：NSW Government 感应支付卡片示例，来源 https://www.nsw.gov.au/easy-read/transport/contactless-payments 。
- `assets/documents/sydney-rail-map.pdf`：Transport for NSW 官方铁路图，来源 https://transportnsw.info/sydney-trains-network-map ，2026-09-19 下载。更新版本可替换同名文件。
- 机场附加费与支付说明按 2026-09-19 核对内容整理；个人经历与官方规则在正文分别说明。

新西兰自驾配图保存在 `assets/images/nz-driving/`，逐图官方来源记录在同目录 `sources.json`。图片包含单／多车道环岛、让行、桥梁、停车及建议速度标志，全部使用本地路径和 `fit: "contain"` 完整展示，离线可读。

行李篇商品示意图来自用户提供的截图，保存在 `assets/images/packing/`，原图保留并以 `fit: "contain"` 完整显示。图中广告价格、品牌和效果宣传不作为博文推荐依据；个人体验以正文为准。

### 悉尼机场餐饮地图

机票首篇新增 `airportMap` 块。`src` 为本地官方地图图片，`pins` 使用百分比 `x`、`y` 定位区域编号，并附 `id`、`label`、`description`。编号表示区域参考，不能当作店铺精确入口；更换地图后必须重核位置。点击地图弹出大图，支持放大、滚动以及关闭按钮、Esc 或点击遮罩关闭，原版 PDF 保存在 `assets/documents/sydney-t1-*.pdf`。

来源： https://www.sydneyairport.com.au/info-sheet/maps 与 https://www.sydneyairport.com.au/eat ，2026-09-20 查询。到达图面日期 30/07/2026；出发图面日期 9/12/2025。原始下载 URL：
- https://assets.ctfassets.net/v228i5y5k0x4/4mBMnHxTdsXLpf6ssxKzUq/9280ace6fef1a752e0f5e9892032db17/T1_International_Arrivals.pdf
- https://images.ctfassets.net/v228i5y5k0x4/4kOmwlY78hJNnGWhAWJtTS/770b373dd0a3e90f85b42aaa98f3cef3/T1_Departures.pdf

### 公共交通实际线路

已核对的线路放在 `assets/transit-routes.js` 的 `window.TRANSIT_ROUTES` 中，包含 `[纬度, 经度]` 坐标数组、线路名、官方来源、核对日期和说明。章节路线用 `mode: 'transit'` 与 `transitRoute: '线路键名'` 引用；地图会画实线，详情提供官方来源链接。不要用汽车路网或直连坐标冒充公交／铁路轨迹。没有核实数据的公共交通保持导航入口；只有明确要显示示意的段落才设置 `displaySchematic: true`。变更交通方式时不会继续套用公共交通线形。

两端点路线可增加 `via: [[纬度, 经度]]` 来约束途经道路，而不把这个位置列成实际停靠站。路线缓存键与导航网址都会包含途经点，总览保留相同约束。


## 临时按地点导入旅行照片

使用桌面 Chrome / Edge 打开本地 `index.html`，进入每日行程，在“当天的足迹”中的地点下面点“＋ 导入照片”。第一次选择本项目的 `assets/photos` 文件夹并允许写入，然后多选该地点的照片。以后在同一次打开中不必重新选择目标文件夹。

- 照片原样复制到 `assets/photos/day-N/`，不修改原始文件；支持 JPG、PNG、WebP、GIF、AVIF，单张上限 50 MB。HEIC 请先转成 JPG。
- `assets/photos/photos.js` 保存日期、地点和照片的关联，刷新页面仍会显示照片。请保留此文件，不要只移动图片。
- 同一地点重复选择同一张照片会跳过；文件采用内容哈希命名，避免重名覆盖。导入完成前不要关闭或刷新页面。
- 点击缩略图查看大图。地点导入按钮仅本地文件页面出现；临时脚本 `local-photo-import.js` 已排除在 Git 外，发布流程也会移除其脚本引用。
- 照片全部整理完成后再统一提交、发布；此阶段没有自动上传 GitHub。

也可以在地点下面点“粘贴照片”，首次授权目标文件夹后，在出现的粘贴框里按 ⌘V / Ctrl+V。支持剪贴板里的截图和图片；纯文字、图片网址或文件路径不能当作图片导入。每次保存后粘贴框会保持选中，可以继续粘贴到同一地点。

还支持直接拖放：把一张或多张照片拖入页面时，每个地点的照片区会出现虚线高亮，当前目标区域为实线高亮。松手到地点卡片或照片区即可导入；首次未授权文件夹时，会先保留这批文件，点击该地点的“选择 photos 文件夹并导入”完成保存。未完成授权前不要刷新。拖到地图或其他空白区不会导入，也不会打开文件覆盖页面。Mac 相册拖出的 Live Photo 可能只有静态部分，目前 MOV / HEIC 会明确提示不支持，不会作为完整 Live 导入。

HEIC 导入已支持：按文件内容识别格式，不依赖相册拖放时的 MIME 值。HEIC 和 MOV/MP4 会先原样保存并显示“原片已保存／待转换”，发布前再转换网页兼容格式；这一步尚不生成 Live Photo 的配对播放。拖放文件会先读取为本次导入副本，再等待目标目录授权，避免依赖相册临时文件。导入结果固定显示在页面右下角。

## 2026-09-22 照片整理完成

已将 103 个导入文件整理为 59 张照片卡片：44 张 Live 动态照片、15 张静态照片，覆盖 28 个地点。网页使用 `assets/photos/web/` 内的 JPG 封面和 MP4 动态视频，关联配置仍为 `assets/photos/photos.js`。点击带 LIVE 标记的卡片播放，默认静音，可用播放器控件开启声音。

所有原片保留在本地 `assets/photos/day-N/`；原始导入关联备份为项目根目录 `.local-photo-originals.json`，这两部分不提交 Git。静态和动态配对优先使用原文件名；第一天不同名的一组通过 Apple content identifier 核对配对。

本轮导入已结束，`index.html` 中的临时导入脚本引用已移除。若以后继续导入，需要先恢复本地入口，再整理新增原片；不要覆盖现有发布配置。
